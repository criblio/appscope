version: '3.5'
services:
  cribl:
    command: -v
    volumes:
      - ./logs:/opt/test-runner/logs
  splunk:
    command: -v
    volumes:
      - ./logs:/opt/test-runner/logs

  nginx:
    command: -v
    volumes:
      - ./logs:/opt/test-runner/logs

  interposed_func:
    command: -v
    volumes:
      - ./logs:/opt/test-runner/logs
