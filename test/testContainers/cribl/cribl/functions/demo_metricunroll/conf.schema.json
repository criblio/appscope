{
    "title": "",
    "type": "object",
    "properties": {
        "includeOrExclude": {
            "title": "Include/Exclude",
            "description": "Dim and Value field patterns either whitelist (include) or blacklist (exclude)",
            "type": "number",
            "enum": [
                0,
                1
            ],
            "enumNames": [
                "Include",
                "Exclude"
            ],
            "default": 0
        },
        "dimFields": {
            "title": "Dimension Fields",
            "description": "Fields to include/exclude as dimensions. Can include wildcards (*)",
            "type": "array",
            "items": {
                "type": "string"
            }
        },
        "valFields": {
            "title": "Value Fields",
            "description": "Fields to include/exclude as values Can include wildcards (*)",
            "type": "array",
            "items": {
                "type": "string"
            }
        }
    }
}