version: '3.5'
services:
  cribl:
    command: -v
    volumes:
      - ./logs:/opt/test-runner/logs
  splunk:

    command: -v
    volumes:
      - ./logs:/opt/test-runner/logs

  nginx:
    command: -v # verbose output
    volumes:
      - ./logs:/opt/test-runner/logs

  interposed_func:
    command: -v
    volumes:
      - ./logs:/opt/test-runner/logs

  elastic:
    command: -v
    volumes:
      - ./logs:/opt/test-runner/logs

  kafka:
    command: -v
    volumes:
      - ./logs:/opt/test-runner/logs

