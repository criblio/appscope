* Change unit tests to only use process execution for the specific run test and mock the file output in other tests
* Tail for Metrics
* Option for run to output metric and/or event data on stderr
* Back out any ID related stuff in favor of a scope generated ID
* Payload support
* TermDash UI
* Support setting verbosity on metrics
* Pull out Viper if we don't find a use for it
* Add CLI to CI/CD
* Port cscope run implementation to golang
* Signal to running process, turn up instrumentation
* Metrics and Events summary view from what came out
* Summary view across runs
* `clean` or `prune` command
* Show command duration in `hist`, show number of events, number of metric events