apiVersion: v1
kind: Service
metadata:
  name: scope-prometheus
spec:
  selector:
    app.kubernetes.io/name: scope-prometheus
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 9100

