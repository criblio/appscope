version: '3.5'
services:
  cribl:
    command: -v
    volumes:
      - ./logs:/opt/test-runner/logs
  splunk:

    volumes:
      - ./logs:/opt/test-runner/logs

  nginx:
    volumes:
      - ./logs:/opt/test-runner/logs

  interposed_func:
    command: -v
    volumes:
      - ./logs:/opt/test-runner/logs
